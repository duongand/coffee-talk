{
  "name": "coffee-talk",
  "version": "1.0.0",
  "main": "",
  "scripts": {
    "dev-start": "concurrently npm:start-client npm:start-server",
    "start-client": "npm --prefix client run watch",
    "start-server": "npm --prefix server run start",
    "install-client": "npm install --prefix client",
    "install-server": "npm install --prefix server",
    "heroku-postbuild": "npm run install-client && npm run install-server"
  },
  "homepage": "https://github.com/duongand/coffee-talk#readme",
  "dependencies": {
    "concurrently": "^7.3.0"
  }
}
